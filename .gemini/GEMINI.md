コードを修正したらエラーがないかを必ず確認しなさい
プッシュする際は必ず許可を取りなさい
URLパス・プレフィックス（canonical URL、openGraph url、ページのファイルパス構造など）を変更する場合は、必ず事前に許可を取ること。勝手にパスを変更してはならない。
コードの変更前に必ず計画を日本語で提示し、コード変更の許可をとりなさい。
npmではなくpnpmを使用してください。
PowerShellでは&&が使えないことに注意してください。
変更を加えるたびにプッシュする必要はない。こちらが求めたときのみプッシュを行ってください。
単語帳を新たに追加するときは".agent\workflows\adding_new_textbook.md"を参照し、ルールに従って追加作業を行いなさい。
既存のユーザーに不具合が出ることは絶対に避けなければいけません。改修を加えるときは既存のシステムに影響がないかを必ず確認し、改修後は必ずチェックしなさい。
Implementation Planは必ず日本語で出力しなさい。
疑問点があれば自分で判断せず、私が求める仕様を質問して確認しなさい。
git add　の前に必ずコンソールエラーがないかを確認しなさい
コード変更後は必ず pnpm build（またはテストコマンド）を実行し、ビルドエラーやリンターエラーが発生しないことを確認してから報告すること。
UIを変更する場合は、既存のレスポンシブデザイン（モバイルファースト）を崩さないこと。新しいコンポーネントを導入する際は、既存のUIライブラリとの整合性を確認すること。
コミットメッセージは、変更内容がひと目でわかるように日本語で簡潔に記述すること（例: feat: 〇〇機能の追加、fix: △△のバグ修正）。
新しいパッケージが必要な場合は必ず相談し、インストール時は必ず pnpm を使用すること。npm用のLockfileを生成してはならない。
.env ファイルを直接編集してはならない。必要な環境変数がある場合は、私に設定を依頼するか、.env.example の更新のみを行うこと。
『使用されていない』と判断したコードやコメントであっても、勝手に削除してはならない。削除が必要な場合は必ず計画段階で提示すること。
新しいファイルやコンポーネントを作成する際は、既存のディレクトリ内の命名規則を分析し、それに完全に合わせること。
実装完了後、デバッグ用の不要な console.log が残っていないか必ず確認し、クリーンな状態で報告すること。
破壊的変更（Breaking Changes）の可能性がある場合は、いかなる小さな修正であっても『影響範囲の調査結果』を日本語で報告しなさい。